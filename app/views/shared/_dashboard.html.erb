<div class="dashboard-banner">
  <div class="total-memories">
    <h2>You currently have <%= pluralize @memories.count, "memory" %></h2>
  </div>
  <div class="conteneur d-none d-md-block d-lg-block">
    <div class="row">
      <div class="col-md-2 offset-md-2">
        <% count1 = [] %>
        <% @memories.each do |memory| %>
          <% if memory.suggested == true %>
            <% count1 << memory %>
          <% end %>
        <% end %>
        <p><%= count1.length %> Musiques</p>
      </div>
      <div class="col-md-2">
        <% count2 = [] %>
        <% @memories.each do |memory| %>
          <% if memory.memory_type == "photo" %>
            <% count2 << memory %>
          <% end %>
        <% end %>
        <p><%= count2.length %> Photos</p>
      </div>
      <div class="col-md-2">
        <% count3 = [] %>
        <% @memories.each do |memory| %>
          <% if memory.memory_type == "video" %>
            <% count3 << memory %>
          <% end %>
        <% end %>
        <p><%= count3.length %> Video</p>
      </div>
      <div class="col-md-2">
        <% count4 = [] %>
        <% @memories.each do |memory| %>
          <% if memory.memory_type == "quote" %>
            <% count4 << memory %>
          <% end %>
        <% end %>
        <p><%= count4.length %> Quotes</p>
      </div>
    </div>
  </div>
</div>
